<form method="post" action="{{ path('app_link_delete', {'id': link.id}) }}"
      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');" class="d-inline">
    <button type="submit" class="btn btn-danger button-uniform">Supprimer</button>
</form>